import { Routes } from '@angular/router';
import { DirectivaComponent } from './directiva.component';

export const DirectivaRoutes: Routes = [
    {
        path:'directiva',
        component: DirectivaComponent
    }
];